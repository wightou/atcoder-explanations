# CHANGELOG

## v70

- DP系の知識記事を `高速化系` に統一。
  - `動的計画法`、`木DP`、`bitDP`、`区間DP`、`DAG上のDP`: `D問題相当` / `高速化系`。
  - `桁DP`、`全方位木DP`: `E問題相当` / `高速化系`。
  - ナップサック問題、最長増加部分列、最長共通部分列など、具体的な問題型は `典型問題集` のまま維持。
- `DAG上のDP` の新規スタブ定義を `E問題相当` から `D問題相当` に変更。
- `scripts/create_knowledge_stubs.py` から、既存 Markdown の front matter 同期・旧 slug リネームなどの一時的な移行処理を削除。
  - スクリプトは未作成の知識記事を生成するだけで、既存の `knowledge/*.md` を変更しない。

## v69

- `区間DP` と `桁DP` を `典型問題集` から外し、`高速化系` に変更。
  - `区間DP`: `D問題相当` / `高速化系`。
  - `桁DP`: `E問題相当` / `高速化系`。
- `scripts/create_knowledge_stubs.py` 実行時、既存の `knowledge/interval-dp.md` と `knowledge/digit-dp.md` も front matter の分類を同期するように変更。

## v68

- `区間DP` と `桁DP` を独立した知識記事の生成対象に追加。
  - `区間DP`: `D問題相当` / `典型問題集` / `knowledge/interval-dp.md`。
  - `桁DP`: `E問題相当` / `典型問題集` / `knowledge/digit-dp.md`。
  - どちらも `動的計画法` を関連知識として登録。
- 知識記事本文の内部リンクを追加。
  - `## 関連アルゴリズム` 節内で、`###` 見出しが既存知識記事の正式な `title` と完全一致する場合、該当記事へのリンクにする。
  - Markdownの1行全体が `詳しくは「XXX」の記事参照。` と完全一致し、`XXX` が正式な `title` と一致する場合、`XXX` だけをリンクにする。
  - aliases / absorbs や、定型文と一致しない文章は自動リンクしない。

## v67

- 表セルの `{red}` などの色指定を、文字色ではなく背景色へ変更。
  - `○`・`×` のような小さい記号でも色分けを視認しやすくする。
  - 文字色は通常の本文色のまま維持する。
  - 背景色は全 22 色とも淡色にし、通常文字と見出し文字の可読性を保つ。
  - CSS class に加えてセルのインライン `style` にも `background-color` を出力する。
  - 記法と対応色名は v63〜v66 と同じで、既存 Markdown の書き換えは不要。

## v66

- 表セル文字色の出力を強化。
  - `{red}` などの色指定を、従来の CSS class に加えてセルのインライン `style` にも出力する。
  - 生成 HTML に色 class が付いていても外部 CSS が古い・未適用な環境で文字色が変わらない問題を回避する。
  - 許可済みの固定色だけを使用し、任意の CSS は受け付けない。
- `explanations/` 配下に `.md`・`.cpp`・`.png` 以外のファイルがある場合、ビルド時に警告するように変更。
  - 実行ファイル、`.exe`、`.bin` などの混入を検出する。
  - 警告のみで、サイト生成は継続する。

## v65

- 各問題ページの日本語タイトルの配置を調整。
  - PCなど横幅の広い画面では、英語タイトル直下の日本語タイトルを右揃えで表示する。
  - 横幅が狭い画面では、読みやすさを優先して日本語タイトルを左揃えに戻す。
  - 英語タイトル、日本語タイトル、下線、問題ページ・解答例リンクの上下関係は v64 のまま維持する。

## v64

- 各問題ページで、front matter の `problem_title_ja` を英語タイトルの直下に表示するように変更。
  - 日本語タイトルは左揃えの補助タイトルとして表示する。
  - 英語タイトルと日本語タイトルを同じ見出し領域に収め、その下に従来の下線と問題ページ・解答例リンクを配置する。
  - `problem_title_ja` が空または未指定の場合は、従来どおり英語タイトルだけを表示する。
- 狭い画面では日本語タイトルの文字サイズを少し小さくし、折り返し可能な表示にした。

## v63

- Markdown表のセル文字色を指定できる独自記法を追加。
  - セルの先頭に `{red}` のような色指定を書くと、そのセル内の文字色を変更する。
  - 見出しセルと通常セルの両方に対応。
  - `markdown` パッケージ使用時と簡易フォールバック変換時の両方で同じ表示になる。
- 白いセル背景と薄青色の見出し背景の双方で読みやすい、22色のパレットを追加。
  - `red`, `coral`, `orange`, `amber`, `yellow`, `olive`, `lime`, `green`, `emerald`, `teal`, `cyan`, `blue`, `navy`, `indigo`, `violet`, `purple`, `magenta`, `rose`, `pink`, `brown`, `gray`, `black`
- 未対応の色名は変換せず、記述ミスを画面上で確認できるようにした。

## v62

- `バックトレース` を独立した知識記事の生成対象として復活。
  - 難易度は `D問題相当`、分類は `データ探索系`。
  - `経路復元`、`解の復元` を別名として登録。
  - `動的計画法`、`最短経路問題` を関連知識として登録。
- `バックトレース` と、深さ優先探索に吸収している `バックトラック` は別概念であることを仕様書に明記。

## v61

- `イベントソート` を独立した知識記事の生成対象から削除。
- `イベントソート` は `シミュレーション` の `absorbs` で吸収する方針に変更。
- 既存の `knowledge/event-sort.md` は自動削除せず、手動削除対象として扱う。

## v59

- 共通HTMLテンプレートに全ページ分として入っていたスクリプトを整理。
- MathJax は、本文Markdownに数式区切りが含まれる解説ページ・知識ページでのみ読み込むように変更。
  - 生成HTML上の `\(` / `\)` / `\[` / `\]` のエスケープ修正は維持。
- `problem_title_ja` の自動縮小 JavaScript は、日本語タイトル付き問題カードを表示するコンテスト表・教育系コンテスト一覧でのみ読み込むように変更。
- トップ、問題解説ポータル、通常リスト型コンテストページ、タグページ、知識一覧ページなどには、不要なスクリプトを出力しないようにした。
- v57 の一時対応で使った括弧専用 `span` などは引き続き撤去済み。
- 現在使っていない旧ヘッダー用の補助関数を削除。

## v58

- MathJax 設定の `\(` / `\)` / `\[` / `\]` のエスケープを根本修正。
  - 生成HTML内で JavaScript に `\(` などが正しく渡るようにし、通常テキスト中の半角括弧が MathJax の数式区切りとして誤処理されないようにした。
- v57 の一時対応だった `problem_title_ja` 内の半角括弧専用 `span` を撤去。
- `problem_title_ja` 周りの不要な括弧補正 CSS と古いコメントを整理。
- 日本語タイトルの1行表示・自動縮小・省略表示、問題番号と日本語タイトルの薄青色は維持。


## v57

- `problem_title_ja` 内の半角括弧 `(` `)` を、画面表示では専用 `span` に分けて描画するように変更。
  - `title` 属性や元データの文字列は `Eリットル (?)` のまま保持。
  - 画面上で薄い文字色・小さい文字サイズの半角括弧だけが見えなくなる問題に対応。
- 問題番号と日本語タイトルの薄青色 `#b8d0ee`、日本語タイトルの1行自動縮小・省略表示は維持。

## v56

- `problem_title_ja` の半角括弧など細い記号が、薄い青色・小さい文字サイズで見えにくくなる問題を軽減。
- 日本語タイトルの文字色 `#b8d0ee` と1行自動縮小・省略表示は維持。
- 日本語タイトルの `unicode-bidi` / `direction` 指定を外し、文字の描画を通常フローに戻した。
- 日本語タイトルだけ、薄さを保ったまま glyph をわずかに補強する CSS を追加。

## v55

- `problem_title_ja` の表示を再調整。
  - 日本語タイトルは1行表示・自動縮小・省略表示を維持。
  - 表示要素をセル幅いっぱいに取り、左右に余白を持たせて、半角括弧などの記号が端で欠けにくいように変更。
- 日本語タイトル専用の `title` 属性を削除。
  - 問題セル全体の hover 表示と、日本語タイトル部分にマウスを乗せたときの表示が食い違わないようにした。
- 問題番号と日本語タイトルの薄青色 `#b8d0ee` は維持。

## v54

- 問題一覧セル内の問題番号と日本語タイトルの色を、薄い青 `#b8d0ee` に変更。
- `problem_title_ja` は引き続き1行表示とし、幅に収まらない場合は自動縮小し、それでも入らない場合は省略表示にする。
- `problem_title_ja` に半角括弧などの記号が含まれる場合、字形の端が欠けにくいように左右の内部余白と方向指定を調整。
- 通常コンテスト表の横幅設定は v51 以降の方針を維持。


## v53

- `problem_title_ja` を1行表示に戻し、折り返さないように変更。
- 日本語タイトルがセル幅に収まらない場合、JavaScript で最小限縮小するように変更。
  - 縮小しても収まらない場合は省略表示にする。
  - hover 時にはブラウザ標準のツールチップで日本語タイトル全文を確認できるようにする。
- 問題番号と日本語タイトルは、v52 よりさらに薄い色に変更。
  - 中央の英語タイトルは通常の本文色のまま維持。
- 通常コンテスト表の横幅設定は v52 / v51 の方針を維持。

## v52

- `problem_title_ja` の表示で、半角括弧などが欠けないように調整。
  - 日本語タイトルは省略表示ではなく、必要に応じて最小限折り返す。
- 問題一覧セル内の補助情報を薄めの色に変更。
  - 左上の問題番号と右下の日本語タイトルに `--subtle` 色を使用。
  - 中央の英語タイトルは通常の本文色のまま維持。
- 通常コンテスト表の横幅は v51 の設定を維持。

## v51

- ABC などの通常コンテスト表について、横幅を v48 相当へ戻すように再調整。
  - `.contest-table` の `min-width` を通常時 `760px`、狭い画面では `720px` に戻した。
  - 中央の英語タイトルを太字ではなく通常の太さに変更。
- `problem_title_ja` 対応と、教育系コンテストページのカード表示は維持。

## v50

- v49 の `problem_title_ja` 表示は維持しつつ、一覧の表示密度を調整。
- ABC などの通常コンテスト表は、v48 に近いセル幅・セル高へ戻し、日本語タイトルがある場合だけ最低限縦に広がるように変更。
- 教育系コンテストページのカードは、v49 のカード形式を維持しつつ、上下幅・余白を小さく変更。

## v49

- 問題解説 Markdown の任意 front matter として `problem_title_ja` に対応。
- ABC などの通常コンテスト表で、各問題セルを3点配置に変更。
  - 左上に `ABC461 A` のような問題コード。
  - 中央に `problem_title` の英語タイトル。
  - 右下に `problem_title_ja` の日本語タイトル。
- 教育系コンテストページを、通常コンテスト表と同じ3点配置のカード一覧に変更。
  - タグは引き続き表示しない。

## v48

- 問題表示で、コンテスト名と問題記号の間に半角スペースを入れるように変更。
  - 個別解説ページ上部を `EDPCB` ではなく `EDPC B` の形で表示。
  - ABC などの問題一覧表も `ABC461A` ではなく `ABC461 A` の形で表示。
  - 教育系コンテスト一覧も同じ表記に統一。

## v47

- 教育系コンテストページを、表ではなくコンテスト別の簡潔な一覧表示に変更。
  - 問題記号とタイトルを1つのリンクにまとめる。
  - タグは表示しない。
- `http://` / `https://` のサイト外リンクに `target="_blank" rel="noopener noreferrer"` を付け、別ウィンドウまたは別タブで開くように変更。
  - サイト内リンクは従来どおり同じウィンドウで開く。

## v46

- `EDPC` / `TDPC` / `NDPC` / `FPS24` を `教育系コンテスト` として扱うように変更。
- `contests/educational.html` を生成し、その中で各コンテストごとに表を分けて表示するように変更。
- Markdown配置は `explanations/edpc/`、`explanations/tdpc/`、`explanations/ndpc/`、`explanations/fps24/` のように別ディレクトリを維持する方針に整理。

## v45

- `C++のコードの書き方` を `難易度問わず` / `基本` の知識記事として追加。
  - slug は `cpp-code-style`。
- 知識ページ右側のサイドバーに `related` から解決した `関連知識` リンクを表示するように変更。
  - `title` / `aliases` / `absorbs` のいずれかで関連先を解決する。
  - 解決できない `related` がある場合は、サイト生成時に警告を表示する。

## v44

- `カタラン数` を `G問題以上相当` / `その他数学系` の知識記事として追加。
  - slug は `catalan-number`。
  - `一般化カタラン数` は `カタラン数` で吸収する。

## v43

- `尺取法` と `sliding window法` を別記事として扱うように変更。
  - `尺取法` の slug は `shakutori-method`。
  - `sliding window法` の slug は `sliding-window`。
- 旧 slug `two-pointers` は `shakutori-method` へリネームするように変更。
- 旧 v41 / v42 の `knowledge/sliding-window.md` が `title: 尺取法` の場合、本文を保持して `knowledge/shakutori-method.md` へ条件付きリネームする処理を追加。
- `scripts/build_site.py` で `docs/search.json` を生成しないように変更。
- `scripts/create_knowledge_stubs.py` の skip 出力は既存ファイルの中身を表示せず、既定では件数だけ表示するように変更。

## v42

- `ツーポインタ法` の category を `高速化系` から `データ探索系` に変更。
- `Moアルゴリズム`、`平方分割`、`分割統治法` の category を `高速化系` に変更。
- `イベントソート` と `貪欲法` は `データ探索系` のまま扱う方針を明記。

## v41

- `尺取法` の slug を `two-pointers` から `sliding-window` に変更。
- 既存の `knowledge/two-pointers.md` は `create_knowledge_stubs.py` 実行時に `knowledge/sliding-window.md` へリネーム。


## v40

- `ツーポインタ法` を `C問題相当` / `高速化系` の知識記事として追加。
- `尺取法` から `two pointers` 系の別名を外し、`ツーポインタ法` との関連に整理。
- 知識カテゴリの表示順を調整し、`典型問題集` を `その他` の下に移動。
- 変更履歴を `CHANGELOG.md` に集約する運用に整理。
- 画像掲載対応、`超頂点`、典型問題集、`階乗`、`二項係数` など、直近のメタ更新を含む。
