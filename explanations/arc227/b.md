---
contest: ARC227
problem: B
problem_title: "Know Your Place"
problem_title_ja: "各自の場所"
problem_url: https://atcoder.jp/contests/arc227/tasks/arc227_b
submission_url: https://atcoder.jp/contests/arc227/submissions/78512719
tags:
  - 考察問題
  - 構築問題
  - 貪欲法
tag_note: 簡単なレベルの内容は省略。
---
## 考え方

仮に $B$ が構成できたとして、値 $i$ がどこに入れられるか考えてみる。
以下、順番は `0-indexed` で表記する。

まず、条件を満たしている以上、$i$ 番目より前は全て $i$ 未満の数。
よって、$i$ 番目以後、次に $i$ 未満の値が初めて登場する前まで。
これが、値 $i$ を使用できる範囲である。

ということは、$i$ 番目は「それまでに未使用な $i$ 以下の数で最大のもの」でよい。

カウンティングソートと `stack` を利用すれば、計算量は $O(N)$ となる。

## 入力例1での動作

入力を受け取る。

```text
n: 4
a: {3, 0, 1, 0}
```

各値の個数を数えると、`counters` は次のようになる。

```text
counters: {2, 1, 0, 1}
```

`que` を、現在使用できる未使用の値を小さい順に持つ `stack` として使う。

$i=0$ では、値 $0$ を $2$ 個 `que` に追加する。
この中で最大の値 $0$ を採用する。

```text
que: {0}
result: {0}
```

$i=1$ では、値 $1$ を $1$ 個 `que` に追加する。
最大の値 $1$ を採用する。

```text
que: {0}
result: {0, 1}
```

$i=2$ では、値 $2$ は存在しないので何も追加しない。
`que` に残っている最大の値 $0$ を採用する。

```text
que: {}
result: {0, 1, 0}
```

$i=3$ では、値 $3$ を $1$ 個 `que` に追加する。
最大の値 $3$ を採用する。

```text
que: {}
result: {0, 1, 0, 3}
```

最後まで構築できたので、`Yes` と `0 1 0 3` を出力する。
## 注意点

特になし。

## 別解

特になし。
